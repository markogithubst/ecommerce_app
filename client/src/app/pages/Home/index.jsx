import HomePage from '../../features/Home';

const Home = () => {
  return (
    <div style={({ width: '75vw', margin: 'auto auto auto auto' })}>
      <HomePage />
    </div>
  );
};

export default Home;
