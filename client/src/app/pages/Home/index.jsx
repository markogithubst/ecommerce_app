import HomePage from '../../features/Home/HomePage';

const Home = () => {
  return (
    <div style={({ width: '75vw', margin: '40px auto auto auto' })}>
      <HomePage />
    </div>
  );
};

export default Home;
